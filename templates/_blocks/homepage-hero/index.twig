<homepage-hero lazy-css="homepage-hero">
    {% set image = data.backgroundImage[0] ?? null %}
    {% if image %}
        <img src="{{ image.url }}" alt="{{ image.description }}" />
    {% endif %}
    <div class="wrapper">
        <h1>{{ data.heading }}</h1>
        {{ data.copy }}
    </div>
</homepage-hero>